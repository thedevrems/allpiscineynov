// Définition d'une fonction pour concaténer deux chaînes de caractères

// Fonction qui prend deux arguments, les convertit en chaînes de caractères si nécessaire, et les concatène
function concatStr(str1, str2) {
    return String(str1) + String(str2);
}