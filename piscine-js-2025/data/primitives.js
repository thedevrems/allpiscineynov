// Déclaration de variables primitives en JavaScript

// Déclare une variable de type chaîne de caractères (string)
const str = "example string";

// Déclare une variable de type nombre (number)
const num = 42;

// Déclare une variable de type booléen (boolean)
const bool = true;

// Déclare une variable de type indéfini (undefined)
const undef = undefined;